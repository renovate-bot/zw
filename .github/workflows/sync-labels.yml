---
name: ♻️ Sync Labels

on:
  push:
    branches:
      - main
    paths:
      - .github/labels.yml
  workflow_dispatch:

jobs:
  labels:
    name: ♻️ Sync labels
    uses: z-shell/.github/.github/workflows/sync-labels.yml@main
